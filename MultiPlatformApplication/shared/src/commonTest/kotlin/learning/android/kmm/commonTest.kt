package learning.android.kmm

import kotlin.test.Test
import kotlin.test.assertTrue

class CommonGreetingTest {

    // A sample test with mocking platform specific dependencies.
    // The dependencies are injected through constructor.
    /* Did not implemented due to issues with mocking.
     It can be implemented but I don't like the way it is happening.
     See RepositoryTest for mocking.
     */
//    @Test
//    fun testExample() {
//        assertTrue(Greeting().greeting().contains("Hello"), "Check 'Hello' is mentioned")
//    }
}